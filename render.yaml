services:
  - type: web
    name: 3d-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.1 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
